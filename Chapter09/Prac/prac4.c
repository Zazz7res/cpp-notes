/*  4．两数的调和平均数这样计算：先得到两数的倒数，
    然后计算两个倒数的平均值，最后取计算结果的倒数。
    编写一个函数，接受两个double类型的参数，返回这两个参数的调和平均数。
*/ 

#include <stdio.h>
double harmonic(double, double);
double reciprocal(double n);
int main(void) {
   printf("请分别输入两个数字，数字之间用空格隔开，输入完成后按回车提交") 

    return 0;
}

double (double x, double y) {

}
