/*  6．设计一个名为alter()的函数，接受两个int类型的变量x和y，
    把它们的值分别改成两个变量之和以及两变量之差。*/
#include <stdio.h>

void alter(int *, int *);

int main(void) {
    int x = 6, y = 5;
    printf("before: x = %d , y = %d\n", x, y);
    alter(&x, &y);
    printf("alter: x = %d, y = %d\n", x, y);
}

void alter(int * x, int * y) {
    int a = *x, b = *y;
    *x = a + b;
    *y = a - b;
}
